info:
  name: GET container
  type: http
  seq: 13

http:
  method: GET
  url: "{{baseUrl}}/container"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/container"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          [
            {
              "id": 0,
              "name": "",
              "description": "",
              "category": "SNACK",
              "capacity": 0,
              "stock": 0
            }
          ]
